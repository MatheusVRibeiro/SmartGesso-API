# SmartGesso API — Regras Hermes

## ANTES DE QUALQUER TAREFA

**Carregue skills relevantes SEMPRE.** Skills obrigatórias:
- `spec-a23` — workflow de implementação
- `nestjs-development` — padrões NestJS (DTOs, controllers, módulos)
- `backend-security-audit` — análise de segurança
- `codebase-health-check` — antes de finalizar
- `thermo-nuclear-code-quality-review` — revisão profunda
- `database-indexing` — para queries e migrations
- `sql-data-migrations` — para migrations SQL

## Stack
- NestJS + Prisma + MySQL
- TypeScript strict
- Auth: JWT + Guards + BFF (cookies HttpOnly)
- Valores financeiros: Decimal(15,2) no Prisma
- DTOs com class-validator

## Regras
- Não misturar tokens/guards de admin com usuários de empresas
- Em rotas mobile, usar companyId da autenticação, nunca do body
- Não versionar .env, segredos ou credenciais
- Sempre executar lint, typecheck, testes e build antes de concluir

## MySQL
- Host: 212.85.3.212:3306
- DB: u815496249_smartgesso
- User: u815496249_smartgesso

## API Admin
- Admin Web roda em Next.js (porta 3001) com BFF auth
- Admin Mobile será React Native
